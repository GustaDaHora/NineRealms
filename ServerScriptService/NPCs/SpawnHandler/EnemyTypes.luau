local RS = game:GetService("ReplicatedStorage")
local NPCModels = RS:WaitForChild("NPCs")
local Modules = game:GetService("ServerStorage"):WaitForChild("Modules")
local NPCsFolder = workspace.Alives.NPCs.Enemies

local EnemyTypes = {
	Boar = {
		modelsFolder = NPCModels:WaitForChild("Boars"),
		spawnFolder = NPCsFolder:WaitForChild("Boars"),
		spawnArea = workspace.Areas.Kornhavn.Kornhavn,
		maxNPCs = 25,
		sound = "AnimalAttack",
		levelRange = { min = 1, max = 6 },
		hitboxSize = Vector3.new(3.5, 3.5, 4.5),
		soulValue = 1,
		spawnHeight = 3,
		punchDistance = 7,
		type = "Dumb",
		dropTable = "Boar",
		spawnRate = 1.5, -- Time in seconds between spawns
	},
	Wolf = {
		modelsFolder = NPCModels:WaitForChild("Wolfs"),
		spawnFolder = NPCsFolder:WaitForChild("Wolfs"),
		spawnArea = workspace.Areas.Veidiskog.Veidiskog,
		maxNPCs = 15,
		sound = "AnimalAttack",
		levelRange = { min = 5, max = 15 },
		hitboxSize = Vector3.new(3.5, 5, 6),
		soulValue = 2,
		spawnHeight = 2,
		punchDistance = 8,
		type = "Smart",
		dropTable = "Wolf",
		spawnRate = 2.0,
	},
	Draugr = {
		modelsFolder = NPCModels:WaitForChild("Draugrs"),
		spawnFolder = NPCsFolder:WaitForChild("Draugrs"),
		spawnArea = workspace.Areas.Deyjaskog.Deyjaskog,
		maxNPCs = 15,
		sound = "OrdinarySwing",
		levelRange = { min = 8, max = 16 },
		hitboxSize = Vector3.new(4, 7, 4),
		soulValue = 3,
		spawnHeight = 2,
		punchDistance = 8,
		type = "Dumb",
		dropTable = "Draugr",
		spawnRate = 2.0,
	},
	Wraith = {
		modelsFolder = NPCModels:WaitForChild("Wraiths"),
		spawnFolder = NPCsFolder:WaitForChild("Wraiths"),
		spawnArea = workspace.Areas.Isdalr.Isdalr,
		maxNPCs = 15,
		sound = "OrdinarySwing",
		levelRange = { min = 15, max = 22 },
		hitboxSize = Vector3.new(4, 7, 4),
		soulValue = 3,
		spawnHeight = 2,
		punchDistance = 8,
		type = "Dumb",
		dropTable = "Wraith",
		spawnRate = 2.0,
	},
	Ravencall = {
		modelsFolder = NPCModels:WaitForChild("Ravencall"),
		spawnFolder = NPCsFolder:WaitForChild("Ravencall Encampment"),
		spawnArea = workspace.Areas["Ravencall Encampment"]["Ravencall Encampment"],
		maxNPCs = 6,
		sound = "OrdinarySwing",
		levelRange = { min = 20, max = 25 },
		hitboxSize = Vector3.new(4, 7, 4),
		soulValue = 5,
		spawnHeight = 2,
		punchDistance = 10,
		type = "Smart",
		dropTable = "Bandit",
		spawnRate = 3.0,
	},
}

return EnemyTypes
